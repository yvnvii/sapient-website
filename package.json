{
  "name": "sapient-journal",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run check:assets",
    "preview": "astro preview",
    "check:assets": "node scripts/check-asset-sizes.mjs dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.0.0"
  }
}
